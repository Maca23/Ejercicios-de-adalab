'use strict';

const age = 33;
const hoursDay = 24;
const daysYear = 365;

const totalHoursLife = (age * hoursDay * daysYear);

console.log('Total horas vividas: ' + totalHoursLife + 'horas vividas');